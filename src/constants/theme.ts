import type { SystemStyleObject } from "styled-system/types";

export const colors = {
	"html-background": "neutral.200",
	"header-background": "neutral.200",
} satisfies Record<string, SystemStyleObject["bg"]>;
